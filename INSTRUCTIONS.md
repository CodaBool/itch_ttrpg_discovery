# primary tags horror role playing tabletop zine one-page rules-lite supplement fanzine micro-rpg vtt ttrpg # secondary tags: > these should also be hit but just marking them as less often used in itch. Keep them in the same array in the app. Just write a comment saying these are less popular - OSR - Tabletop - body-horror - liminal-horror - mothership - mothership-rpg - tabletop-role-playing-game - foundryvtt - panic-engine - generation - generated - generator - tool # Genre > genres seem to effectively work as tags for our purposes, only care about rpg and adventure genres. Genre: rpg Genre: Adventure # Category > each category has a certain format which is used in the URL [use-this-value] in the base part of the url Category: Assets [game-assets] Category: Physical Game [physical-games] Category: Other [misc] Category: Game mod [game-mods] Category: Tool [tools] # Example https://itch.io/CATEGORY/newest/last-day/tag-$TAG # design - run cron ~2min - use url for dedup, adding to category + tag rows when a dup is found. # single table items url (item.link) source (json, has the categories and tags and where it was from) title (item.plainTitle) description (item.description, in a xml format) image_url (item.imageurl) price (iftem.price) publish_date (item.pubDate) update_date (item.updateDate) author (item.guid.split("https://")[1].split("itch.io/")[0]) author_url ("https://" + fetchedUrl.split("https://")[1].split("/")[0]) first_seen_at (set this when running the cron, don't update on tag updates) updated_at (d1 entry updated at)